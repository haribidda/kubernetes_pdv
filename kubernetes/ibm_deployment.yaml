apiVersion: apps/v1
kind: Deployment
metadata:
  name: pdv

spec:
  replicas: 1
  selector:
    matchLabels:
      app: pdv
  template:
    metadata:
      labels:
        app: pdv

    spec:
      containers:
        - name: pdv
          image: jp.icr.io/ns-v2/pdv
          imagePullPolicy: Always
          ports:
            - containerPort: 5000
              protocol: TCP
